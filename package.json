{
  "name": "fe-note",
  "version": "1.0.0",
  "description": "",
  "main": "createstyle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1", // 222

    /*
     *  防止项目编译ing 不可用的问题 需要配合server端使用 让server加载 非webpack打包目录的内容
     *  '/static/(.*)': '../view/'
     */
    "build": "vue-cli-service build && cp -r ./dist/. ./view",
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vsionly/fe-note.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/vsionly/fe-note/issues"
  },
  "homepage": "https://github.com/vsionly/fe-note#readme",
  "dependencies": {
    "node-xlsx": "^0.15.0"
  }
}
